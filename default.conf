server {
    listen 80;

    # Serve App 1
    location /app1 {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Serve App 2
    location /app2 {
        root /usr/share/nginx/html;
        index index.html;
    }
}
